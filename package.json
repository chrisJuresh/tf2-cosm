{
  "name": "tf2-cosm",
  "private": true,
  "type": "module",
  "scripts": {
    "build-catalogue": "pnpm --filter @tf2-cosm/data build-catalogue",
    "dev": "pnpm --filter @tf2-cosm/site dev",
    "build-site": "pnpm --filter @tf2-cosm/site build",
    "test": "pnpm --recursive test",
    "typecheck": "pnpm --recursive typecheck"
  }
}
